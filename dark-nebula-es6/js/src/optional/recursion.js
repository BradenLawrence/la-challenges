listFiles = (data, dirName) => {
  // Optional Challenge
};

permute = (arr) => {
  // Optional Challenge
};

fibonacci = (n) => {
  // Optional Challenge
};

validParentheses = (n) => {
  // Optional Challenge
};
